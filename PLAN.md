# 健身 App 产品与技术方案

> 双端（iOS + Android）| 核心功能：今天练什么 / AI 分部位训练计划 / 饮食计算 / 减脂计划

---

## 1. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 客户端 | **React Native + Expo**（TypeScript） | 一套代码出 iOS/Android 双端；EAS 一键打包发布；支持热更新；UI 库和生态成熟。若团队更熟 Dart 可换 Flutter，架构不变 |
| UI | Tamagui / NativeWind + Reanimated | 健身 App 需要计时器、图表、动画，这些库覆盖 |
| 后端 | **Supabase**（Postgres + Auth + Storage + Edge Functions） | MVP 阶段最省事，自带登录/数据库/文件存储；若主要面向国内用户需备案+国内云（腾讯云/阿里云 + Node.js/NestJS 自建），架构不变 |
| AI | 服务端代理调用大模型（国内：GLM / DeepSeek / 通义；海外：GPT / Claude），**结构化输出（JSON Schema）** | App 不直连模型，key 在服务端，方便换模型和控制成本 |
| 图表 | Victory Native / react-native-svg | 体重曲线、热量环 |

## 2. 功能模块设计

### 模块一：今天练什么（首页核心）

- **Onboarding 问卷**（一次性）：目标（增肌/减脂/塑形）、训练经验（新手<1年/中级1-3年/老手>3年）、每周可练几天、器械条件（健身房/家庭哑铃/纯徒手）、身高体重年龄性别
- 系统按答案生成**周期化训练分化**（Split）：
  - 每周 2~3 天 → 全身训练
  - 每周 4 天 → 上下肢分化
  - 每周 5~6 天 → 推拉腿（PPL）或胸背腿肩臂分化
- 打开 App 首页直接呈现**今日课表**：动作、组数×次数、建议负重、组间休息时间
- **训练执行模式**：组间计时器、逐组记录重量×次数、RPE 自评
- 简单恢复规则：同一肌群间隔 ≥48~72 小时才再次安排；漏练自动顺延
- 「换一天」按钮：不想练安排的部位时可一键换

### 模块二：AI 分部位训练计划（差异化卖点）

- 部位选择：胸 / 背 / 肩 / 手臂 / 腿（股四头、腘绳肌、臀）/ 核心，二级细分肌群（如胸 → 上胸/中胸/外沿）
- **关键架构：不让 AI 编造动作**。内置结构化动作库（300~500 个动作），每个动作含：名称、目标肌群（主/辅）、器械要求、难度、动作要领、常见错误、演示 gif
- AI 的职责是**编排**：输入（部位 + 肌群侧重 + 器械条件 + 水平 + 时长 + 近期训练记录）→ LLM 从动作库中选动作、定组数次数、排顺序，输出 JSON
- 结构化输出示例：
  ```json
  {
    "plan_name": "胸肌侧重上胸 45 分钟",
    "exercises": [
      { "exercise_id": 102, "name": "上斜杠铃卧推", "sets": 4, "reps": "8-10", "rest_sec": 120, "note": "坡度30°，重点感受上胸发力" }
    ],
    "ai_tips": "你这周二次练胸，降低容量以中重量为主……"
  ]
  ```
- 单个动作不满意 → 一键替换为同肌群同器械的备选动作（纯规则，不耗 AI 调用）
- 计划可保存、可收藏、可设为「今日训练」

### 模块三：饮食计算

- 基础代谢 BMR：**Mifflin-St Jeor 公式**
  - 男：10×体重kg + 6.25×身高cm − 5×年龄 + 5
  - 女：10×体重kg + 6.25×身高cm − 5×年龄 − 161
- 每日消耗 TDEE = BMR × 活动系数（1.2 久坐 / 1.375 轻度 / 1.55 中度 / 1.725 高度）
- 目标热量：减脂 −300~500 kcal；增肌 +200~300 kcal；且**不低于 BMR**
- 宏量分配：蛋白质 1.6~2.2 g/kg 体重，脂肪 0.8~1 g/kg，剩余给碳水
- 食物库 + 三餐记录：每日热量环（已摄入/剩余）、三大营养素进度条
- 训练日/休息日热量差异化（可选，减脂期碳水循环）
- 二期：拍照识别食物估算热量（多模态模型）

### 模块四：减脂计划

- 输入：当前体重（可含体脂率）、目标体重、期望周期、活动水平
- 输出：
  - 安全约束：每周减重 0.5%~1% 体重，热量缺口不超标，**拒绝过度节食**
  - 每日热量与宏量目标（联动模块三）
  - 每周训练安排：力量训练（保肌肉）+ 有氧（心率区间建议）
  - 阶段里程碑（每 2~4 周一个小目标）
- 执行追踪：每日体重曲线、围度记录、腰臀比；打卡日历
- **平台期检测**：连续 2~3 周体重无变化 → AI 复盘（饮食记录完整度、训练完成度）并给出调整方案（再降 100~200 kcal 或增加有氧/加入重新喂养日）

## 3. 数据模型（核心表）

```
profiles        用户档案：身体数据、目标、经验、器械条件、活动系数
exercises       动作库：肌群标签、器械、难度、要领、gif
workout_plans   训练计划（AI 生成或模板）：分化类型、起止日期
plan_days       计划中的训练日：日期、部位、状态(待练/完成/跳过)
plan_exercises  训练日中的动作：动作ID、组数、次数、休息
workout_logs    实际训练记录；set_logs 每组重量×次数×RPE
foods           食物库：每100g热量与宏量
food_logs       饮食记录：餐次、食物、克数
body_metrics    体重/体脂/围度时间序列
```

## 4. 开发路线

| 阶段 | 内容 | 周期 |
|---|---|---|
| **P0 设计** | 页面原型（Figma）、动作库数据准备、UI 风格定稿 | 1~2 周 |
| **P1 MVP** | Onboarding + 今天练什么（预设模板，不接 AI）+ 训练记录 + 饮食计算（公式版） | 4~6 周 |
| **P2 AI 化** | 接入大模型、AI 分部位计划生成、动作替换、减脂计划 + 体重追踪 | 3~4 周 |
| **P3 打磨** | 拍照识餐、Apple Health / 华为运动健康同步、手表、打卡与激励机制 | 持续迭代 |

MVP 先用规则和模板跑通闭环，AI 后置——既省成本，也避免早期被模型质量拖累核心体验。

## 5. 成本与注意事项

- **账号**：Apple 开发者 $99/年；Google Play $25 一次；国内上架需软著 + 安卓各商店单独审核
- **模型成本**：一次计划生成约 1~2k tokens，按用户量估算；做结果缓存 + 动作替换用规则不耗 AI
- **动作 gif 版权**：不要爬取商业网站素材；可用自录、外包拍摄、或购买授权
- **健康免责声明**：训练与饮食建议需加免责声明；减脂热量下限、特殊人群（孕期/伤病）提示
- **国内合规**（如面向国内）：域名备案、隐私政策、用户协议、数据存国内
